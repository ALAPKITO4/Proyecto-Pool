# 🔧 Cambios Técnicos Específicos en script.js\n\n## 📝 Resumen de Modificaciones\n\n**Archivo:** `script.js`\n**Fecha:** 19 de Abril de 2026\n**Versión:** 2.0 - Sistema Real de Estados\n\n---\n\n## 📍 Función 1: `confirmPool()` - Crear Pool con Participantes\n\n### Ubicación\nAlrededor de línea ~820\n\n### Cambio\nAhora crea un array `participantes` donde cada padre se agrega como participante con un `estado`.\n\n### Antes\n```javascript\nconst newPool = {\n  // ...\n  participants: appState.parents,\n  confirmations: {}\n};\n```\n\n### Ahora\n```javascript\nconst newPool = {\n  // ...\n  participantes: appState.parents.map((parent, index) => ({\n    nombre: parent,\n    telefono: index === 0 ? currentUser.telefono : '',\n    estado: index === 0 ? 'aceptado' : 'pendiente',\n    acceptedAt: index === 0 ? new Date().toISOString() : null,\n    rejectedAt: null\n  })),\n  creatorName: currentUser.nombre,\n  creatorPhone: currentUser.telefono,\n  // ... resto igual\n};\n```\n\n### Nuevo Guardado en Firestore\n```javascript\nif (FIREBASE_ENABLED && window.db) {\n  try {\n    await firebase.firestore()\n      .collection('pools')\n      .doc(String(newPool.id))\n      .set(newPool);\n    console.log('✅ Pool guardado en Firestore');\n  } catch (error) {\n    console.error('Error guardando pool:', error);\n  }\n}\n```\n\n---\n\n## 📍 Función 2: `acceptPoolInvitation()` - Aceptar con Firestore\n\n### Ubicación\nAlrededor de línea ~1300\n\n### Cambios Principales\n\n#### 1. Obtener poolId\n```javascript\nconst urlParams = new URLSearchParams(window.location.search);\nconst poolId = parseInt(urlParams.get('poolId')) || appState.poolId;\n```\n\n#### 2. Buscar en participantes (no invitados)\n```javascript\n// ANTES\nevent.invitados = event.invitados.filter(i => !isNameMatch(i, currentUser.nombre));\n\n// AHORA\nlet found = false;\nevent.participantes = event.participantes.map(p => {\n  if (isNameMatch(p.nombre, currentUser.nombre)) {\n    p.estado = 'aceptado';\n    p.telefono = currentUser.telefono;\n    p.acceptedAt = new Date().toISOString();\n    found = true;\n  }\n  return p;\n});\n```\n\n#### 3. Guardar en Firestore (NUEVO)\n```javascript\ntry {\n  if (FIREBASE_ENABLED && window.db) {\n    const poolRef = firebase.firestore()\n      .collection('pools')\n      .doc(String(poolId));\n    \n    await poolRef.update({\n      participantes: event.participantes,\n      lastUpdated: new Date().toISOString()\n    });\n    \n    console.log('✅ Guardado en Firestore - Participantes actualizados');\n  }\n} catch (error) {\n  console.error('❌ Error guardando:', error);\n  localStorage.setItem(STORAGE_KEY_EVENTS, JSON.stringify(poolsEvents));\n}\n```\n\n#### 4. Mostrar resumen\n```javascript\nshowParticipantsSummary(poolId);\nshowNotification('✅ Aceptaste la invitación!', 'success');\n```\n\n---\n\n## 📍 Función 3: `rejectPoolInvitation()` - Rechazar (NUEVA/MEJORADA)\n\n### Ubicación\nAlrededor de línea ~1401\n\n### Ahora es Async (IMPORTANTE)\n```javascript\nasync function rejectPoolInvitation() {\n  // ...\n}\n```\n\n### Cambios\n\n#### 1. Obtener poolId\n```javascript\nconst urlParams = new URLSearchParams(window.location.search);\nconst poolId = parseInt(urlParams.get('poolId')) || appState.poolId;\n```\n\n#### 2. Buscar y marcar como rechazado\n```javascript\nevent.participantes.forEach(p => {\n  if (isNameMatch(p.nombre, currentUser.nombre)) {\n    p.estado = 'rechazado';\n    p.rejectedAt = new Date().toISOString();\n  }\n});\n```\n\n#### 3. Guardar en Firestore (NUEVO)\n```javascript\nif (FIREBASE_ENABLED && window.db) {\n  try {\n    const poolRef = firebase.firestore()\n      .collection('pools')\n      .doc(String(poolId));\n    \n    await poolRef.update({\n      participantes: event.participantes,\n      lastUpdated: new Date().toISOString()\n    });\n    \n    console.log('✅ Rechazo guardado en Firestore');\n  } catch (error) {\n    console.error('❌ Error guardando rechazo:', error);\n    localStorage.setItem(STORAGE_KEY_EVENTS, JSON.stringify(poolsEvents));\n  }\n}\n```\n\n#### 4. Mostrar resumen\n```javascript\nif (poolId) {\n  showParticipantsSummary(poolId);\n}\n\nshowNotification('👋 Invitación rechazada', 'info');\n```\n\n---\n\n## 📍 Función 4: `subscribeToPoolUpdates()` - Sincronización en Tiempo Real\n\n### Ubicación\nAlrededor de línea ~1560\n\n### Cambios\n\n#### 1. Crear listener\n```javascript\nif (!window._poolUnsubscribers) {\n  window._poolUnsubscribers = {};\n}\n\nif (window._poolUnsubscribers[poolId]) {\n  window._poolUnsubscribers[poolId]();\n}\n```\n\n#### 2. Escuchar cambios\n```javascript\nconst poolRef = firebase.firestore()\n  .collection('pools')\n  .doc(String(poolId));\n\nconst unsubscribe = poolRef.onSnapshot((doc) => {\n  if (doc.exists) {\n    const event = doc.data();\n    \n    // Actualizar poolsEvents\n    poolsEvents = poolsEvents.map(e => \n      e.id === poolId ? event : e\n    );\n    \n    // Mostrar en Console\n    console.log('🔄 Pool actualizado en tiempo real: ' + event.location);\n    console.log('📊 Participantes: ' + \n      event.participantes\n        .map(p => `${p.nombre}(${p.estado})`)\n        .join(', ')\n    );\n    \n    // Actualizar UI\n    goToStep(11);\n  }\n});\n```\n\n#### 3. Guardar referencia\n```javascript\nwindow._poolUnsubscribers[poolId] = unsubscribe;\nconsole.log('✅ Sincronización en tiempo real activa');\n```\n\n---\n\n## 📍 Función 5: `showParticipantsSummary()` - Resumen de Estados (NUEVA)\n\n### Ubicación\nAlrededor de línea ~1545 (después de `subscribeToPoolUpdates()`)\n\n### Código Completo\n```javascript\nfunction showParticipantsSummary(poolId) {\n    const event = poolsEvents.find(e => e.id === poolId);\n    if (!event || !event.participantes) return;\n    \n    const total = event.participantes.length;\n    const aceptados = event.participantes.filter(p => p.estado === 'aceptado').length;\n    const rechazados = event.participantes.filter(p => p.estado === 'rechazado').length;\n    const pendientes = total - aceptados - rechazados;\n    \n    console.log(`\\n📊 RESUMEN DE PARTICIPANTES - Pool: ${event.location}`);\n    console.log(`   ✅ Aceptados: ${aceptados}/${total}`);\n    console.log(`   ⏳ Pendientes: ${pendientes}/${total}`);\n    console.log(`   ❌ Rechazados: ${rechazados}/${total}`);\n    console.log(`   Participantes: ${event.participantes.map(p => `${p.nombre}(${p.estado})`).join(', ')}\\n`);\n}\n```\n\n### Se llama desde\n- `acceptPoolInvitation()` - Después de guardar\n- `rejectPoolInvitation()` - Después de guardar\n- `showPoolDetails()` - Al abrir detalles\n\n---\n\n## 📍 Función 6: `showPoolDetails()` - Mostrar Estados en UI\n\n### Ubicación\nAlrededor de línea ~1720\n\n### Cambios\n\n#### 1. Usar participantes (no participants)\n```javascript\n// ANTES\nconst participants = event.participants || [];\n\n// AHORA\nconst participantes = event.participantes || [];\n```\n\n#### 2. Mostrar estado con badge\n```javascript\nconst participantsHTML = participantes.map(p => {\n  let estadoBadge = '';\n  let estadoColor = '#999';\n  \n  if (p.estado === 'aceptado') {\n    estadoBadge = '✅ Aceptado';\n    estadoColor = '#4CAF50';\n  } else if (p.estado === 'rechazado') {\n    estadoBadge = '❌ Rechazado';\n    estadoColor = '#f44336';\n  } else {\n    estadoBadge = '⏳ Pendiente';\n    estadoColor = '#FF9800';\n  }\n  \n  return `\n    <div style=\"display: flex; justify-content: space-between; padding: 8px;\">\n      <span>${p.nombre}</span>\n      <span style=\"color:${estadoColor}; font-weight: bold;\">${estadoBadge}</span>\n    </div>\n  `;\n}).join('');\n\ndocument.getElementById('participantsList').innerHTML = participantsHTML;\n```\n\n#### 3. Mostrar resumen\n```javascript\nshowParticipantsSummary(poolId);\ngoToStep(11);\n```\n\n---\n\n## 📍 Función 7: `checkForSharedPool()` - Activar Sincronización\n\n### Ubicación\nAlrededor de línea ~1906\n\n### Cambios\n\n#### 1. Guardar poolId en appState\n```javascript\nappState.poolId = poolId;\n```\n\n#### 2. Activar subscripción (NUEVO)\n```javascript\nif (FIREBASE_ENABLED && window.db && poolId) {\n  subscribeToPoolUpdates(poolId);\n}\n```\n\n#### 3. Mostrar creador\n```javascript\nconst creatorEl = document.getElementById('invCreatedBy');\nif (creatorEl) {\n  creatorEl.textContent = event.creatorName || event.createdBy || 'Desconocido';\n}\n```\n\n---\n\n## 🔗 Relaciones Entre Funciones\n\n```\nconfirmPool()\n  ├─ Crea participantes[]\n  ├─ Guarda en Firestore\n  └─ Llama a showParticipantsSummary()\n\ncheckForSharedPool()\n  ├─ Encuentra el pool\n  ├─ Guarda poolId en appState\n  ├─ Llama a subscribeToPoolUpdates()  ← SINCRONIZACIÓN ACTIVA\n  └─ Muestra Step-10\n\nsubscribeToPoolUpdates()\n  ├─ Crea onSnapshot listener\n  ├─ Actualiza poolsEvents en tiempo real\n  ├─ Muestra participantes en Console\n  └─ Reinderiza Step-11 si está visible\n\nacceptPoolInvitation()\n  ├─ Busca usuario en participantes[]\n  ├─ Cambia estado a \"aceptado\"\n  ├─ Guarda en Firestore\n  └─ Llama a showParticipantsSummary()\n\nrejectPoolInvitation()\n  ├─ Busca usuario en participantes[]\n  ├─ Cambia estado a \"rechazado\"\n  ├─ Guarda en Firestore\n  └─ Llama a showParticipantsSummary()\n\nshowPoolDetails()\n  ├─ Lee participantes[] del pool\n  ├─ Renderiza con estado badges\n  ├─ Llama a showParticipantsSummary()\n  └─ Abre Step-11\n```\n\n---\n\n## 🔍 Búsqueda Rápida de Líneas\n\n| Función | Línea Aproximada | Cambio |\n|---------|-----------------|--------|\n| `confirmPool()` | 820 | Crear `participantes` array |\n| `acceptPoolInvitation()` | 1300 | Guardar en Firestore |\n| `rejectPoolInvitation()` | 1401 | Nuevo: hacer async y guardar |\n| `subscribeToPoolUpdates()` | 1560 | Agregar listeners onSnapshot |\n| `showParticipantsSummary()` | 1545 | Nueva función helper |\n| `showPoolDetails()` | 1720 | Mostrar badges de estado |\n| `checkForSharedPool()` | 1906 | Activar subscripción |\n\n---\n\n## 🧪 Cómo Verificar los Cambios\n\n### 1. Verificar confirmPool\n```javascript\nconst pool = poolsEvents[0];\nconsole.log(pool.participantes);\n// Debe mostrar array con objetos de participantes\n```\n\n### 2. Verificar subscribeToPoolUpdates\n```javascript\nconsole.log(window._poolUnsubscribers);\n// Debe mostrar objeto con listeners activos\n```\n\n### 3. Verificar Firestore Guardado\n```javascript\n// En Firebase Console\npools/[poolId]/participantes\n// Verificar que estados cambien en tiempo real\n```\n\n---\n\n## ⚠️ Consideraciones Importantes\n\n1. **rejectPoolInvitation() es async**: Necesita `await` o `.then()`\n\n2. **onSnapshot se dispara múltiples veces**: Es normal, cada cambio en Firestore\n\n3. **Backward compatibility**: Mantiene `participants`, `confirmations`, `invitados` arrays por compatibilidad\n\n4. **Firebase es requerido**: Sin Firebase, solo funciona localStorage\n\n5. **poolId debe estar en appState o URL**: Sino no se pueden guardar cambios\n\n---\n\n**Versión Técnica:** 2.0\n**Última Actualización:** 19 de Abril de 2026\n